import Foundation

/// Placeholder for the local-notification + Live Activity wrapper used to show
/// initialization progress/completion (plan.md §12, task.md S14). Display-only:
/// per plan.md §12 this must never be used as a background compute container.
///
/// Implemented in task.md S14.1-S14.2. Left empty for now so the `UI/` group
/// compiles as part of the S1 scaffolding step.
public enum WorkspaceNotificationSupport {}
